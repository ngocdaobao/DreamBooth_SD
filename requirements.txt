# Requirements for train_dreambooth_sdxl_lora.py on Kaggle
# Kaggle pre-installs: torch, torchvision, PIL, numpy, pandas, etc.
# This file includes only packages that need to be installed or upgraded

# Core diffusion & transformers (may need update)
diffusers
transformers>=4.36.0
safetensors>=0.4.0

# Parameter-efficient fine-tuning (LoRA)
peft>=0.9.0

# Distributed training & mixed precision
accelerate>=0.27.0

# Optimization utilities
bitsandbytes>=0.42.0  # For 8-bit Adam (optional but recommended)

ftfy>=6.1.1  # For text cleaning in tokenizers
opencv-python>=4.8.0  # Image processing (often pre-installed)
wandb>=0.16.0  # For logging (optional, if using --report_to wandb)
datasets
tensorboard
Pillow
numpy
peft
scipy
torch
torchvision
huggingface-hub>=0.34.0
tqdm
protobuf
deepspeed
einops
controlnet_aux
gdown
insightface
onnxruntime


